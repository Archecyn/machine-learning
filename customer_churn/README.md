# customer_churn

The 'customer_churn' project was generated by using the default-python template.

* `src/`: Python source code for this project.
  * `src/customer_churn/`: Shared Python code that can be used by jobs and pipelines.
  * `src/churn_model/`: Churn prediction model modules (validation, feature engineering, etc.)
* `resources/`:  Resource configurations (jobs, pipelines, etc.)
* `tests/`: Unit tests for the shared Python code.
* `fixtures/`: Fixtures for data sets (primarily used for testing).
* `databricks.yml`: Databricks Asset Bundle configuration for deployment.


## Project Structure

```
customer_churn/
├── src/
│   ├── customer-churn.ipynb          # Main notebook
│   └── churn_model/
│       ├── validate_model.ipynb      # Model output validation
│       ├── engineer_features.ipynb  # Feature engineering
│       ├── calculate_churn.ipynb     # Churn calculation
│       ├── churn_model.ipynb         # Model training
│       └── config.ipynb              # Configuration
├── resources/
│   └── scheduled_job.yml             # Databricks job definition
├── tests/
│   ├── churn_test.py                 # Unit tests
│   └── conftest.py                   # Test fixtures
├── fixtures/                         # Test data fixtures
├── databricks.yml                    # Asset bundle definition
└── pyproject.toml                    # Project dependencies
```


## Databricks Asset Bundle

This project uses **Databricks Asset Bundles** for deployment management. The bundle configuration is defined in [databricks.yml](databricks.yml).

### Deployment Targets

| Target | Mode | Description |
|--------|------|-------------|
| `uat` | Development | Default target for testing/development |
| `prd` | Production | Production deployment with permissions |

### Bundle Variables

| Variable | Description |
|----------|-------------|
| `cpu_cluster_id_small` | Small cluster for continuous batches |
| `cpu_cluster_id_medium` | Medium cluster for Digital Jobs |
| `cpu_cluster_id_large` | Large cluster for Digital jobs |
| `pause_status` | Job schedule status (PAUSED/UNPAUSED) |
| `job_email_notifications` | Email notifications for job events |


## Getting started

Choose how you want to work on this project:

(a) Directly in your Databricks workspace, see
    https://docs.databricks.com/dev-tools/bundles/workspace.

(b) Locally with an IDE like Cursor or VS Code, see
    https://docs.databricks.com/dev-tools/vscode-ext.html.

(c) With command line tools, see https://docs.databricks.com/dev-tools/cli/databricks-cli.html

If you're developing with an IDE, dependencies for this project should be installed using uv:

*  Make sure you have the UV package manager installed.
   It's an alternative to tools like pip: https://docs.astral.sh/uv/getting-started/installation/.
*  Run `uv sync --dev` to install the project's dependencies.


# Using this project using the CLI

The Databricks workspace and IDE extensions provide a graphical interface for working
with this project. It's also possible to interact with it directly using the CLI:

1. Authenticate to your Databricks workspace, if you have not done so already:
    ```
    $ databricks configure
    ```

2. To deploy a development copy of this project, type:
    ```
    $ databricks bundle deploy --target uat
    ```
    (Note that "uat" is the default target for development.)

    This deploys everything that's defined for this project.
    For example, the default template would deploy a pipeline called
    `[dev yourname] customer_churn_etl` to your workspace.
    You can find that resource by opening your workpace and clicking on **Jobs & Pipelines**.

3. Similarly, to deploy a production copy, type:
   ```
   $ databricks bundle deploy --target prd
   ```
   Note the default template has a includes a job that runs the pipeline every day
   (defined in resources/scheduled_job.yml). The schedule
   is paused when deploying in development mode (see
   https://docs.databricks.com/dev-tools/bundles/deployment-modes.html).

4. To run a job or pipeline, use the "run" command:
   ```
   $ databricks bundle run
   ```

5. Finally, to run tests locally, use `pytest`:
   ```
   $ uv run pytest
   ```


## Running Tests

Run unit tests to validate the model validation logic:

```bash
# Using uv
$ uv run pytest

# Or directly with pytest
$ pytest tests/
```

### Test Coverage

- **validate_model**: Tests for DataFrame validation (empty check, required columns, prediction values, probability range, null values)
